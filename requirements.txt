# Install the package in editable mode with all dev dependencies:
#
#   pip install -e ".[dev]"
#
# This installs choreo-mini + pytest + langgraph (needed for conversion tests).
#
# Optional backend extras (install only what you need):
#
#   pip install -e ".[langgraph]"   # LangGraph + langchain-mcp-adapters
#   pip install -e ".[crewai]"      # CrewAI + crewai-tools
#   pip install -e ".[autogen]"     # AutoGen + autogen-ext
#
# Runtime dependencies are declared in pyproject.toml [project.dependencies]
# and are installed automatically with any of the above commands.
